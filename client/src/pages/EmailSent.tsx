import React from "react";
import styled from "styled-components";

const Base = styled.div``;

const EmailSent: React.FC = () => {
  return <Base>등록하신 이메일로 확인 메일이 보내졌습니다.</Base>;
};

export default EmailSent;
