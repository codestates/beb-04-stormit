import React from "react";
import styled from "styled-components";

const Base = styled.div``;

const CommentCard: React.FC = () => {
  return <Base>CommentCard</Base>;
};

export default CommentCard;
